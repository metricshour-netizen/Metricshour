# PostgreSQL 17 tuning for Netcup (12 CPU / 32GB RAM)
# Apply these overrides in /etc/postgresql/17/main/postgresql.conf

shared_buffers = 8GB
effective_cache_size = 18GB
work_mem = 64MB
maintenance_work_mem = 1GB
max_connections = 100
